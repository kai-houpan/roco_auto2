# 洛克王国：世界 — 自动孵蛋工具设计文档

> 版本: 1.0
> 日期: 2026-05-14
> 状态: 待开发

---

## 1. 项目概述

### 1.1 目标

开发一款 Windows 桌面工具，自动化完成游戏《洛克王国：世界》中的孵蛋及咕噜球契约流程。用户通过 GUI 面板选择目标蛋种和咕噜球类型后，程序在游戏孵蛋界面循环执行"放蛋 → 等待孵化 → 契约"的操作。

### 1.2 核心能力

- 基于 OpenCV 模板匹配识别游戏 UI 元素
- 通过 PyAutoGUI 模拟鼠标点击与滚轮操作
- 提供 Tkinter 图形界面，支持蛋/球选择、启停控制和实时日志
- 健壮的错误处理与重试机制，异常时自动停止并记录原因

---

## 2. 技术栈

| 组件         | 技术                                | 用途                         |
| ------------ | ----------------------------------- | ---------------------------- |
| 图像识别     | OpenCV (cv2)                        | 模板匹配，定位游戏 UI 元素   |
| 输入模拟     | PyAutoGUI                           | 鼠标移动、点击、滚轮、键盘   |
| GUI 框架     | Tkinter                             | 控制面板、日志展示           |
| 配置管理     | Markdown (.md) 字典表               | 蛋和咕噜球的名称-图像映射    |
| 运行环境     | Windows 11, Python 3.10+            |                              |

---

## 3. 运行环境

| 项目             | 规格                            |
| ---------------- | ------------------------------- |
| 操作系统         | Windows 11 (64-bit)             |
| 屏幕分辨率       | 2560 × 1440                    |
| 缩放比例         | 100%（不可更改）                |
| 游戏窗口尺寸     | 1442 × 938（含标题栏）          |
| 游戏窗口状态     | 窗口化模式，保持在孵蛋界面      |

---

## 4. 项目结构

```
roco_auto2/
├── DESIGN.md                  # 本设计文档
├── main.py                    # 程序入口，启动 GUI
├── core/
│   ├── __init__.py
│   ├── automator.py           # 主状态机，编排整个工作流
│   ├── recognizer.py          # OpenCV 模板匹配封装
│   ├── operator.py            # PyAutoGUI 鼠标/键盘操作封装
│   └── window_manager.py      # 游戏窗口定位与管理
├── ui/
│   ├── __init__.py
│   ├── app.py                 # Tkinter 主窗口
│   ├── control_panel.py       # 控制面板（蛋/球选择、启停按钮）
│   └── log_panel.py           # 日志展示面板
├── config/
│   ├── __init__.py
│   ├── dictionary_loader.py   # 字典文件解析器
│   └── settings.py            # 全局常量与配置
├── assets/
│   └── reference/
│       ├── reference_diagram/ # 流程 UI 元素模板图
│       └── reference_list/   # 蛋和咕噜球的模板图及字典
│           ├── eggs/
│           └── gulus/
└── logs/                      # 运行日志输出目录
```

---

## 5. 状态机工作流

### 5.1 状态总览

```
┌─────────────────────────────────────────────────────────┐
│                      START                              │
│               定位游戏窗口，置顶                            │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
          ┌───────────────────────┐
          │   STATE_2             │
          │   检测孵蛋装置是否空缺   │
          │   noegg_selected?      │
          └───┬───────────────┬───┘
              │ YES           │ NO
              ▼               ▼
    ┌─────────────┐   ┌──────────────┐
    │  STATE_3    │   │  STATE_6     │
    │  放入蛋      │   │  检测未选中   │
    │  putin?     │   │  noegg_not-  │
    └──┬──────────┘   │  selected?   │
       │              └──┬───────┬───┘
       ▼                 │ YES   │ NO
    ┌──────────┐         ▼       ▼
    │ STATE_4  │   STATE_2  ┌──────────────┐
    │ 选择蛋   │            │  STATE_7     │
    │ (滚动)   │            │  检测孵化完成 │
    └──┬───────┘            │  finished_   │
       │                    │  selected?   │
       ▼                    └──┬───────┬───┘
    ┌──────────┐               │ YES   │ NO
    │ STATE_5  │               ▼       ▼
    │ 确认放入  │          ┌──────────┐ ┌──────────────┐
    │ putin_1? │          │ STATE_8  │ │  STATE_12    │
    └──┬───────┘          │ 咕噜球   │ │ 检测未选中    │
       │                  │ contract?│ │ finished_    │
       └──► STATE_2       └──┬───────┘ │ notselected? │
                             │         └──┬───────────┘
                             ▼            │ YES → STATE_8
                        ┌──────────┐      │
                        │ STATE_9  │      │
                        │ 选择球   │      │
                        │ (滚动)   │      │
                        └──┬───────┘      │
                           │
                           ▼
                        ┌──────────┐
                        │ STATE_10 │
                        │ 确认契约  │
                        │ contract_1?
                        └──┬───────┘
                           │
                           ▼
                        ┌──────────┐
                        │ STATE_11 │
                        │ 点击确认  │
                        │ click?   │
                        └──┬───────┘
                           │
                           └──► STATE_2 (循环)
```

### 5.2 状态详细说明

#### STATE_1 — 定位窗口

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 触发条件   | 工作流启动 / 窗口位置发生变化                                  |
| 操作       | 使用 `window.png` 模板匹配定位游戏窗口左上角坐标，记录为 `(win_x, win_y)` |
| 失败处理   | 若未找到窗口，停止工作流，日志输出 "窗口未找到，工作流停止"     |
| 成功后     | `pyautogui.click(win_x + 721, win_y + 469)`（窗口中心），确保窗口获得焦点 |

#### STATE_2 — 检测孵蛋装置空缺

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `noegg_selected.png`                                         |
| 识别区域   | 全屏                                                         |
| 匹配阈值   | 0.8（可调）                                                  |
| 匹配成功   | 获取匹配坐标 `(x, y)`，鼠标点击该坐标                         |
| 验证方式   | 点击后检测 `putin.png` 是否出现                               |
| 验证成功   | 进入 STATE_3                                                  |
| 验证失败   | 重试点击（最多 3 次），仍失败则返回 STATE_2 重新检测           |
| 匹配失败   | 进入 STATE_6（可能装置已满或已在孵化中）                      |

#### STATE_3 — 放入蛋

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `putin.png`                                                  |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测 `putin_wait.png` 是否出现                          |
| 验证成功   | 进入 STATE_4                                                  |
| 验证失败   | 重试点击（最多 3 次），仍失败则返回 STATE_2                   |

#### STATE_4 — 选择蛋

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | 用户选择的蛋对应的图像文件名（如 `shenqi_egg.png`）            |
| 识别区域   | 游戏窗口内部（`win_x` ~ `win_x + 1442`, `win_y` ~ `win_y + 938`），缩小搜索范围提升性能 |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测对应 `_selected` 图像（如 `shenqi_egg_selected.png`） |
| 验证成功   | 进入 STATE_5                                                  |
| 验证失败   | 重试点击（最多 3 次），仍失败返回 STATE_4 重新搜索             |
| 匹配失败   | 鼠标移动到相对窗口坐标 `(1160, 500)`，向下滚动 24 齿，重试     |
| 重试上限   | **10 次**，超过后停止工作流，日志输出 "未找到目标蛋 [名称]，工作流停止" |

#### STATE_5 — 确认放入

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `putin_1.png`                                                |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测 `takeout.png` 是否出现                             |
| 验证成功   | 返回 STATE_2（检测下一个装置）                                 |
| 验证失败   | 重试点击（最多 3 次），仍失败返回 STATE_4                      |

#### STATE_6 — 检测未选中空缺装置

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `noegg_notselected.png`                                      |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测 `noegg_selected.png` 是否出现                      |
| 验证成功   | 返回 STATE_2                                                  |
| 验证失败   | 重试点击（最多 3 次），仍失败返回 STATE_6                      |
| 匹配失败   | 进入 STATE_7                                                  |

#### STATE_7 — 检测孵化完成

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `finished_selected.png`                                      |
| 检测频率   | 不存在时，每 **0.3 秒** 重新扫描                              |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测 `contract.png` 是否出现                            |
| 验证成功   | 进入 STATE_8                                                  |
| 验证失败   | 重试点击（最多 3 次），仍失败返回 STATE_7                      |
| 匹配失败   | 进入 STATE_12                                                 |

#### STATE_8 — 咕噜球契约入口

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `contract.png`                                               |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测 `contract_wait.png` 是否出现                       |
| 验证成功   | 进入 STATE_9                                                  |
| 验证失败   | 重试点击（最多 3 次），仍失败返回 STATE_7                      |

#### STATE_9 — 选择咕噜球

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | 用户选择的咕噜球对应的图像文件名（如 `putong_gulu.png`）       |
| 识别区域   | 游戏窗口内部                                                  |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测对应 `_selected` 图像（如 `putong_gulu_selected.png`） |
| 验证成功   | 进入 STATE_10                                                 |
| 验证失败   | 重试点击（最多 3 次），仍失败返回 STATE_9 重新搜索             |
| 匹配失败   | 鼠标移动到相对窗口坐标 `(1160, 500)`，向下滚动 24 齿，重试     |
| 重试上限   | **10 次**，超过后停止工作流，日志输出 "未找到目标咕噜球 [名称]，工作流停止" |

#### STATE_10 — 确认契约

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `contract_1.png`                                             |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后**轮询检测** `click.png`（每 0.5 秒一次，最长等待 8 秒），以适应游戏约 5 秒的契约动画 |
| 验证成功   | 进入 STATE_11                                                 |
| 验证失败   | 8 秒超时后重试点击 `contract_1.png`（最多 3 次），仍失败则返回 STATE_9 重新搜索咕噜球 |

#### STATE_11 — 最终确认

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `click.png`                                                  |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测 `site.png` 是否出现                                |
| 验证成功   | 返回 STATE_2（下一轮循环）                                     |
| 验证失败   | 重试点击（最多 3 次）                                         |
| 重试上限   | 3 次后停止工作流                                              |
| 匹配失败   | 持续检测（每 0.3 秒），直到出现或手动停止                     |

#### STATE_12 — 检测未选中完成装置

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 识别目标   | `finished_notselected.png`                                   |
| 匹配成功   | 获取匹配坐标，鼠标点击                                        |
| 验证方式   | 点击后检测 `finished_selected.png` 是否出现                   |
| 验证成功   | 进入 STATE_8                                                  |
| 验证失败   | 重试点击（最多 3 次），仍失败返回 STATE_12                     |
| 匹配失败   | 返回 STATE_7 继续检测（说明没有孵化完成的蛋）                  |

---

## 6. 窗口管理策略

### 6.1 初始定位

程序启动时使用 `window.png` 进行全屏模板匹配，找到游戏窗口左上角坐标 `(win_x, win_y)`。

### 6.2 运行时窗口追踪

每次执行模板匹配时，同时快速校验窗口位置是否发生偏移（用 `window.png` 做轻量校验）。若偏移，更新 `(win_x, win_y)`，日志输出 "窗口位置已更新: (x, y)"。

### 6.3 窗口丢失处理

若连续 **5 次** 校验均无法匹配 `window.png`，判定窗口丢失（被关闭或完全遮挡），立即停止工作流，日志输出 "游戏窗口丢失，工作流停止，请检查游戏状态"。

---

## 7. 图像识别规范

### 7.1 模板匹配参数

| 参数       | 默认值  | 说明                     |
| ---------- | ------- | ------------------------ |
| 算法       | TM_CCOEFF_NORMED | 归一化相关系数匹配 |
| 阈值       | 0.8     | 匹配置信度阈值，低于此值视为未找到 |
| 搜索区域   | 全屏 / 窗口区域 | 性能敏感场景限定在窗口区域 |

### 7.2 参考图像清单

#### 流程图（reference_diagram）

| 文件名                    | 用途                         |
| ------------------------- | ---------------------------- |
| window.png                | 游戏窗口定位                 |
| noegg_selected.png        | 孵蛋装置空缺 - 已选中        |
| noegg_notselected.png     | 孵蛋装置空缺 - 未选中        |
| putin.png                 | "放入"按钮                   |
| putin_wait.png            | 放入后的等待确认             |
| putin_1.png               | 选蛋后的确认放入按钮         |
| takeout.png               | 放入成功后的界面标识         |
| finished_selected.png     | 孵化完成 - 已选中            |
| finished_notselected.png  | 孵化完成 - 未选中            |
| contract.png              | "契约"按钮                   |
| contract_wait.png         | 契约确认等待                 |
| contract_1.png            | 选球后的确认契约按钮         |
| click.png                 | 契约流程中的确认点击         |
| site.png                  | 最终确认后的界面标识         |

#### 蛋种（reference_list/eggs）

| 文件名                    | 中文名称     |
| ------------------------- | ------------ |
| shenqi_egg.png            | 神奇的蛋     |
| shenqi_egg_selected.png   | 神奇的蛋-选中 |
| qilicao_egg.png           | 奇丽草的蛋   |
| qilicao_egg_selected.png  | 奇丽草的蛋-选中 |

#### 咕噜球（reference_list/gulus）

| 文件名                      | 中文名称       |
| --------------------------- | -------------- |
| putong_gulu.png             | 普通咕噜球     |
| putong_gulu_selected.png    | 普通咕噜球-选中 |
| gaoji_gulu.png              | 高级咕噜球     |
| gaoji_gulu_selected.png     | 高级咕噜球-选中 |

---

## 8. 数据字典规范

### 8.1 字典文件格式

`eggs_dictionary.md` 和 `gulus_dictionary.md` 使用 Markdown 表格，程序在启动时解析。

表格结构（以蛋为例）：

```markdown
| 蛋的名称   | 图像              | 检验图像                   |
| ---------- | ----------------- | -------------------------- |
| 神奇的蛋   | shenqi_egg.png    | shenqi_egg_selected.png    |
| 奇丽草的蛋 | qilicao_egg.png   | qilicao_egg_selected.png   |
```

### 8.2 解析规则

- 跳过表头分隔行（以 `|---` 开头）
- 每行提取 3 列：中文名称、图像文件名、检验图像文件名
- 图像文件路径相对于 `assets/reference/reference_list/eggs/` 或 `.../gulus/`
- 解析时 trim 每列的首尾空格
- 新增条目只需追加新行即可，无需修改代码

### 8.3 规模预估

| 类别     | 当前数量 | 预估上限 |
| -------- | -------- | -------- |
| 蛋       | 2        | ~1000    |
| 咕噜球   | 2        | ~100     |

字典加载采用**启动时一次性解析**，存入内存列表。列表较大时（> 50 项），GUI 下拉选择框自动切换为带搜索过滤的下拉组件。

---

## 9. GUI 设计

### 9.1 布局

```
┌─────────────────────────────────────┐
│  🥚 洛克王国自动孵蛋工具            │
├─────────────────────────────────────┤
│                                     │
│  选择目标蛋:   [▼ 神奇的蛋      ]   │
│  选择咕噜球:   [▼ 普通咕噜球    ]   │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │  ▶ 启动  │  │  ⏹ 停止  │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  状态: ● 运行中                     │
│  循环次数: 42                       │
│                                     │
├─────────────────────────────────────┤
│  ┌─ 运行日志 ───────────────────┐   │
│  │ [14:02:01] START 工作流启动   │   │
│  │ [14:02:01] OK   窗口定位成功  │   │
│  │ [14:02:02] OK   noegg_selected│   │
│  │ [14:02:03] OK   putin 匹配成功│   │
│  │ [14:02:04] FAIL 未找到 shenqi │   │
│  │ [14:02:04] ACT  滚动蛋列表    │   │
│  │ [14:02:05] OK   shenqi_egg   │   │
│  │ ...                          │   │
│  │                              │   │
│  └──────────────────────────────┘   │
├─────────────────────────────────────┤
│ 日志文件: logs/20260514_140200.log  │
└─────────────────────────────────────┘
```

### 9.2 交互规则

- **启动按钮**: 仅在停止状态下可点击；点击后按钮变灰
- **停止按钮**: 仅在运行状态下可点击；点击后发送停止信号，当前操作完成后停止
- **下拉选择**: 启动前可选择蛋和球，启动后锁定不可更改
- **状态指示**: 绿色圆点+运行中 / 红色圆点+已停止

### 9.3 配色方案

| 元素       | 颜色         | 色值     |
| ---------- | ------------ | -------- |
| 主背景     | 深灰         | #2B2B2B  |
| 面板背景   | 中灰         | #3C3F41  |
| 文字       | 浅灰白       | #BBBBBB  |
| 日志-成功  | 绿色         | #6A8759  |
| 日志-失败  | 橙红         | #CC7832  |
| 日志-操作  | 蓝色         | #6897BB  |
| 启动按钮   | 绿色         | #4CAF50  |
| 停止按钮   | 红色         | #F44336  |

---

## 10. 日志规范

### 10.1 日志级别

| 前缀  | 含义               | 示例                                          |
| ----- | ------------------ | --------------------------------------------- |
| START | 工作流启动/停止    | `START 工作流启动`                            |
| OK    | 图像匹配成功       | `OK noegg_selected.png 匹配成功 (960, 540)`   |
| FAIL  | 图像匹配/验证失败  | `FAIL shenqi_egg.png 未匹配 (重试 3/10)`      |
| ACT   | 执行操作           | `ACT 点击 (1200, 700)`                        |
| SCROLL| 滚轮操作           | `SCROLL 向下 24齿 相对窗口(1160, 500)`        |
| STOP  | 工作流停止及原因   | `STOP 未找到目标蛋 神奇的蛋 (重试耗尽)`        |
| WARN  | 警告（不影响运行） | `WARN 窗口位置已偏移，已重新定位`              |
| ERROR | 异常错误           | `ERROR OpenCV 读取模板失败: xxx.png`           |

### 10.2 日志输出目标

1. **GUI 日志面板**: 实时滚动显示，最近 500 条，支持暂停滚动
2. **日志文件**: 保存至 `logs/YYYYMMDD_HHMMSS.log`，程序每次启动创建新文件

### 10.3 日志条目格式

```
[HH:MM:SS.mmm] <LEVEL> <message>
```

---

## 11. 错误处理与边界情况

### 11.1 重试策略汇总

| 场景                     | 重试次数 | 耗尽后行为             |
| ------------------------ | -------- | ---------------------- |
| 点击后验证失败（常规）   | 3 次     | 返回上级状态重新识别   |
| 蛋列表滚动搜索           | 10 次    | 停止工作流             |
| 咕噜球列表滚动搜索       | 10 次    | 停止工作流             |
| STATE_10 动画等待        | 8 秒超时 | 重试点击 contract_1（最多 3 次），仍失败返回 STATE_9 |
| STATE_11 点击验证        | 3 次     | 停止工作流             |
| 窗口丢失连续检测         | 5 次     | 停止工作流             |

### 11.2 操作间隔

为避免操作过快导致游戏 UI 未及时响应，所有操作之间加入延迟：

| 操作类型             | 延迟             |
| -------------------- | ---------------- |
| 点击后等待           | 0.5 秒           |
| 滚轮后等待           | 0.3 秒           |
| 轮询扫描间隔         | 0.3 秒           |
| 动画等待（STATE_10） | 最长 8 秒，每 0.5 秒轮询 |

### 11.3 边界情况

| 情况                     | 处理方式                                     |
| ------------------------ | -------------------------------------------- |
| 游戏未启动               | 启动时提示 "请先启动游戏并进入孵蛋界面"       |
| 用户手动停止             | 完成当前操作后安全退出，不残留鼠标状态         |
| 程序崩溃                 | 日志文件记录到崩溃前最后一条操作               |
| 字典文件缺失或格式错误   | 启动时弹窗提示具体错误，拒绝启动               |
| 参考图像文件缺失         | 启动时校验所有图像可读，缺失则列出并拒绝启动   |
| 蛋/球种类为空            | 启动时弹出"请确认字典文件包含至少一个蛋/球"     |

---

## 12. 已知待修复问题

以下问题需在开发前修正：

| # | 问题                                   | 文件                             | 修复方案                          |
| - | -------------------------------------- | -------------------------------- | --------------------------------- |
| 1 | 文件名不匹配                           | `eggs_dictionary.md`             | `qili_egg.png` 改为 `qilicao_egg.png` |
| 2 | 扩展名缺失                             | `gulus_dictionary.md`            | `putong_gulu_selected` 改为 `putong_gulu_selected.png` |

---

## 13. 开发计划

| 阶段 | 内容                                      | 产出物                                     |
| ---- | ----------------------------------------- | ------------------------------------------ |
| P1   | 修复字典文件问题，启动时资源校验           | 可正确加载所有资源的启动脚本                |
| P2   | 核心识别引擎（OpenCV 模板匹配封装）        | `recognizer.py` - 含阈值、区域搜索         |
| P3   | 操作层（鼠标点击、滚轮、延迟）             | `operator.py`                              |
| P4   | 窗口管理（定位、追踪、丢失检测）           | `window_manager.py`                        |
| P5   | 状态机编排（12个状态的完整流程）           | `automator.py`                             |
| P6   | GUI 控制面板 + 日志面板                    | `ui/` 全部文件                             |
| P7   | 集成测试（各状态独立测试 + 完整流程测试）  | 测试通过，可交付使用                        |

---

## 附录 A — 状态转换速查表

| 当前状态 | 条件           | 下一状态 |
| -------- | -------------- | -------- |
| START    | 窗口定位成功   | STATE_2  |
| STATE_2  | noegg_selected 匹配 | STATE_3 |
| STATE_2  | noegg_selected 不匹配 | STATE_6 |
| STATE_3  | putin 匹配验证 | STATE_4  |
| STATE_3  | 验证失败x3     | STATE_2  |
| STATE_4  | 蛋匹配验证     | STATE_5  |
| STATE_4  | 滚动x10 耗尽   | STOP     |
| STATE_5  | putin_1 匹配验证 | STATE_2 |
| STATE_6  | noegg_notselected 匹配 | STATE_2 |
| STATE_6  | 不匹配         | STATE_7  |
| STATE_7  | finished_selected 匹配 | STATE_8 |
| STATE_7  | 不匹配         | STATE_12 |
| STATE_8  | contract 匹配验证 | STATE_9 |
| STATE_9  | 球匹配验证     | STATE_10 |
| STATE_9  | 滚动x10 耗尽   | STOP     |
| STATE_10 | contract_1 点击 + 轮询 click.png（8s超时） | STATE_11 |
| STATE_10 | 轮询超时 x3                               | STATE_9  |
| STATE_11 | click 匹配验证 | STATE_2  |
| STATE_11 | 验证失败x3     | STOP     |
| STATE_12 | finished_notselected 匹配 | STATE_8 |
| STATE_12 | 不匹配         | STATE_7  |

## 附录 B — 改进后的步骤4说明

> 4. 识别图像"用户UI界面选择的蛋"（由用户在控制面板的下拉框中选择，例如选择"神奇的蛋"，程序查找字典中对应行，取图像列文件名 `shenqi_egg.png` 作为匹配目标），若匹配成功，获取该图像坐标，鼠标点击该坐标（通过识别字典中对应的检验图像列 `shenqi_egg_selected.png` 判断鼠标点击是否成功），执行步骤5；若匹配失败，鼠标移动到相对窗口位置 `(1160, 500)` 处向下滑动鼠标滚轮 24 齿后，重新执行步骤4。滚动重试最多 10 次，超过则停止工作流并记录日志。

> 9. 识别图像"用户UI界面选择的咕噜球"（匹配逻辑同步骤4，名称→字典→文件名→模板匹配），若匹配成功，获取该图像坐标，鼠标点击该坐标（通过字典中对应的检验图像判断鼠标点击是否成功），执行步骤10；若匹配失败，鼠标移动到相对窗口位置 `(1160, 500)` 处向下滑动鼠标滚轮 24 齿后，重新执行步骤9。滚动重试最多 10 次，超过则停止工作流并记录日志。
