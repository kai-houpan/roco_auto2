项目的目的是实现游戏《洛克王国：世界》自动孵蛋。
需要使用的技术：
OpenCV (cv2) — 使用模板匹配（Template Matching）
鼠标键盘操作
PyAutoGUI — 负责鼠标移动、点击、拖拽、键盘输入
keyboard 库 
UI 框架
Tkinter — Python 标准库中的 GUI 框架

主要流程如下：游戏窗口会一直保持在孵蛋界面，程序检测孵蛋装置是否空缺，如果空缺就放入蛋，反之就等待孵蛋完成。检测到孵蛋完成就使用咕噜球建立契约。之后继续检测孵蛋装置是否空缺，依次循环。
识别内容主要有游戏内的UI按键,蛋的选择以及咕噜球的选择，使用提供的截图比对，具体是reference_diagram文件夹中的图像。
windows系统分辨率为2560*1440，缩放为100%
游戏窗口尺寸是1442*938（包括窗口化上方的标签），示例为window.png在根据坐标进行鼠标点击时注意先确定与窗口的相对位置。

具体细节逻辑流程如下：
1.确定游戏窗口位置，点击游戏窗口保持游戏进程在最上方。
2.识别图像noegg_selected.png，若存在，获取该图像坐标，鼠标点击该坐标（通过识别putin.png判断鼠标点击是否成功），执行3；若不存在，执行6；
3.识别图像putin.png，若存在，获取该图像坐标，鼠标点击该坐标（通过识别putin_wait.png判断鼠标点击是否成功），执行4；若不存在，执行2；
4.识别图像“用户UI界面选择的蛋”（例如：神奇的蛋，对应图像shenqi_egg.png），可选择eggs_dictionary.md列表中的蛋，若存在，获取该图像坐标，鼠标点击该坐标（通过识别shenqi_egg_selected.png判断鼠标点击是否成功），执行5；若不存在，鼠标在相对窗口位置（1160，500）处向下滑动鼠标滚轮24齿后，执行4；
5.识别图像putin_1.png，若存在，获取该图像坐标，鼠标点击该坐标（通过识别takeout.png判断鼠标点击是否成功），执行2；若不存在，执行4；
6.识别图像noegg_notselected.png，若存在，获取该图像坐标，鼠标点击该坐标（通过识别noegg_selected.png判断鼠标点击是否成功），执行2；若不存在，执行7；
7.识别图像finished_selected.png，若存在，获取该图像坐标，鼠标点击该坐标（通过识别contract.png判断鼠标点击是否成功），执行8；若不存在，执行12；
8.识别图像contract.png，若存在，获取该图像坐标，鼠标点击该坐标（通过识别contract_wait.png判断鼠标点击是否成功），执行9；若不存在，执行7；
9.识别图像“用户UI界面选择的咕噜球”（例如：普通咕噜球，对应图像putong_gulu.png），可选择gulus_dictionary.md列表中的咕噜球，若存在，获取该图像坐标，鼠标点击该坐标，（识别图像putong_gulu_selected.png判断鼠标点击是否成功）执行10；若不存在，鼠标在相对窗口位置（1160，500）处向下滑动鼠标滚轮24齿后，执行9；
10.识别图像contract_1.png，若存在，获取该图像坐标，鼠标点击该坐标（识别图像click.png判断鼠标点击是否成功），执行11；若不存在，执行9；
11.识别图像click.png，若存在获取该图像坐标，鼠标点击该坐标（识别图像site.png判断鼠标点击是否成功），执行2；若不存在，执行11；
12.识别图像finished_notselected.png，获取该图像坐标，鼠标点击该坐标（识别图像finished_selected.png判断鼠标点击是否成功），执行8；
注：以上如果鼠标点击判断失败，就重复点击

UI要求如下：
外观简单，配色协调；
日志能够展示每次逻辑判定进程。
可以让用户预选需要孵化的蛋以及契约的咕噜球，其中内容在eggs_dictionary.md和gulus_dictionary.md中；



